// 점수 계산
function solution(arr) {
  let answer = 0,
  //   cnt = 0;
  // let flag = 1;
  // for (let i = 0; i < arr.length; i++) {
  //   if (arr[i] === 1) {
  //     if (arr[i - 1] === 1) {
  //       flag += 1;
  //       answer += flag;
  //     } else {
  //       answer += flag;
  //     }
  //   } else {
  //     flag = 1;
  //   }
  // }
  return answer;
}

let arr = [1, 0, 1, 1, 1, 0, 0, 1, 1, 0];
console.log(solution(arr));

// inf
function solution(arr) {
  // let answer = 0,
  //   cnt = 0;
  // for (let x of arr) {
  //   if (x === 1) {
  //     cnt++;
  //     answer += cnt;
  //   } else cnt = 0;
  // }

  return answer;
}

let arr = [1, 0, 1, 1, 1, 0, 0, 1, 1, 0];
console.log(solution(arr));
